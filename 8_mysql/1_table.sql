--> comando utilizado para criar tabelas CREATE TABLES (colunas + tipo de dados);
CREATE TABLE usuarios (
    nome VARCHAR(30),
    email VARCHAR(70),
    idade INT
);
--> tipos de dados mais comuns:
--> CHAR
--> VARCHAR()
--> INT/FLOAT
--> DATE

--> obs: Os códigos criados em SQL nesta pasta, são apenas para fins didáticos e para copiar e colar dentro do mysql no cmd, pois aqui fica mais fácil de estruturar, depois só copia e cola exatamente o código no terminal.